#name: A workflow for my Hello World file
#on: push
#
#jobs:
#  build:
#    name: Hello world action
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@master
#        with:
#          ref: demo # name of the branch to check out
#      - uses: ./action-a
#        env:
#          MY_NAME: "Mona"


name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master
        with:
          ref: demo
      - name: Build
        run: cargo build --verbose
      - name: Run tests
        run: cargo test --verbose
        